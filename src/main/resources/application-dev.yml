spring:
  docker:
    compose:
      file: docker-compose.yml
      enabled: true
      lifecycle-management: start_only
management:
  endpoint:
    beans:
      access: read_only
  endpoints:
    web:
      exposure:
        include: '*'